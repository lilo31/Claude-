<div style="min-height: 80vh; display: flex; align-items: center; justify-content: center; text-align: center; padding: var(--space-16);">
  <div>
    <p style="font-family: var(--font-heading); font-size: clamp(4rem, 15vw, 10rem); font-weight: 300; color: var(--color-surface-2); line-height: 1; margin-bottom: var(--space-6);">404</p>
    <h1 style="font-family: var(--font-heading); font-size: clamp(var(--text-3xl), 4vw, var(--text-5xl)); font-weight: 400; margin-bottom: var(--space-5);">
      Page Not Found
    </h1>
    <p style="color: var(--color-text-secondary); max-width: 400px; margin: 0 auto var(--space-10); line-height: 1.7;">
      The page you're looking for doesn't exist or has been moved.
    </p>
    <div style="display: flex; gap: var(--space-4); justify-content: center; flex-wrap: wrap;">
      <a href="/" class="btn btn--primary btn--lg">Go Home</a>
      <a href="/collections/all" class="btn btn--outline btn--lg">Shop All</a>
    </div>
  </div>
</div>
