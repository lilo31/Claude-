<div class="page-header">
  <div class="container">
    {% render 'breadcrumb' %}
    <h1 class="page-header__title">{{ page.title }}</h1>
  </div>
</div>

<div class="container">
  <div class="page-content">
    {{ page.content }}
  </div>
</div>
